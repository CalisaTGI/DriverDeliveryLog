
  # Driver Delivery Log

  This is a code bundle for Driver Delivery Log. The original project is available at https://www.figma.com/design/5MSd7B7OEvb4gaxw1dliHi/Driver-Delivery-Log.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  